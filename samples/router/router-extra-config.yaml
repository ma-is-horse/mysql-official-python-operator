# router-with-extra-config.yaml里用到的configMap
apiVersion: v1
kind: ConfigMap
metadata:
  name: mysqlrouter-extra-config
  namespace: default
data:
  mysqlrouter.conf: |
    [io]
    backend=linux_epoll
    threads=1
